spring:
  application:
    name: gulimall-gateway

  cloud:
    nacos:
      config:
        server-addr:  127.0.0.1:8848
        namespace: public
        ext-config[0].data-id: gulimall-gateway.yml
        ext-config[0].group: DEFAULT_GROUP
        ext-config[0].refresh: true